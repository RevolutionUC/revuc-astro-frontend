---
interface Props {
  imgSrc: string;
  imgAlt: string;
}

const { imgSrc, imgAlt }: Props = Astro.props;
---

<img
  src={imgSrc}
  alt={imgAlt}
  class="h-[500px] w-[760px] object-cover pb-8 max-md:h-[300px] max-md:w-11/12"
  decoding="async"
  loading="eager"
/>
