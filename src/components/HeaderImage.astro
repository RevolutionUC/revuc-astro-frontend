---
interface Props {
  imgSrc: string;
  imgAlt: string;
}

const { imgSrc, imgAlt }: Props = Astro.props;
---

<img
  src={imgSrc}
  alt={imgAlt}
  class="h-[500px] w-11/12 object-cover pb-8 max-md:h-[300px] md:max-w-3xl"
  decoding="async"
  loading="eager"
/>
