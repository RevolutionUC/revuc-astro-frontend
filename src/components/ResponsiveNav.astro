---
import NavBar from "./nav/NavBar.astro";
import NavHamburg from "./nav/NavHamburg.astro";

interface Props {
    current: string;
}

const { current } = Astro.props;
---

<header>
    <NavHamburg current={current} />
	<NavBar current={current} />
</header>